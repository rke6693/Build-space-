{
  "name": "squishymon-echoes-of-lumenfen",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A tactile, story-driven squish-platformer built for the browser with no runtime dependencies.",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test \"tests/unit/*.test.mjs\"",
    "check:syntax": "node tests/syntax-check.mjs",
    "check:static": "node tests/static-checks.mjs",
    "check": "npm run check:syntax && npm run check:static && npm test"
  },
  "keywords": [
    "game",
    "platformer",
    "canvas",
    "web-audio",
    "zero-dependency"
  ]
}
